{
  "name": "over18",
  "private": true,
  "version": "0.1.0",
  "description": "Over18 AI companion — monorepo (web, api, shared)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "npm run build -w packages/shared",
    "dev": "npm-run-all --parallel dev:web dev:api",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "build": "npm run build -w packages/shared && npm run build -w apps/web && npm run build -w apps/api",
    "typecheck": "npm run build -w packages/shared && npm run typecheck -w apps/web && npm run typecheck -w apps/api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
